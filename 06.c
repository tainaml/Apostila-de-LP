#include <stdio.h> 

int main () {

int b,a,area;

printf ("Base do retangulo: ");
scanf("%d", &b);
printf("Altura do retangulo: ");
scanf("%d", &a);

area = b*a;

printf("Area do retangulo: %d ",area);

return 0;

}
